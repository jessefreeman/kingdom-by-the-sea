import type { KBTSApi } from './types';
import { KB } from './compat';

export const T = () => KB().T;
export const C = () => KB().C;
export const LABEL = () => KB().LABEL;
export const DIRS = () => KB().DIRS;
